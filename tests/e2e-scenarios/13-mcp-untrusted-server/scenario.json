{
  "name": "13-mcp-untrusted-server",
  "description": "Stages a .claude/settings.local.json declaring an MCP server pulled from a non-pinned npx invocation with no signature verification. mcp playbook must fire mcp-version-without-integrity and unsigned-mcp-manifest indicators.",
  "verb": "run",
  "args": ["mcp", "--evidence", "evidence.json", "--session-id", "e2e-13", "--force-overwrite", "--json"],
  "expect_exit": 0
}
